# Use a different API origin in production when frontend and API are not same-origin.
# VITE_API_BASE_URL=

# Vite dev server forwards relative /api calls here.
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
