# Secrets / runtime
.env
*.log

# Tool stores that land at repo root (regardless of which stack ran them)
.pnpm-store/
.playwright-mcp/

# Screenshot artifacts dropped at repo root by Playwright / dev scripts
/frontend-*.png
/route-planner-*.png
